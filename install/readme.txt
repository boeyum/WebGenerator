The zip file in this directory is a installation for a windows platform.
The application require a full JAVA installation with posibilities to run JAVA from command line.

Unzip the .zip file the place you want it and run the .bat file.

If you running it on a MAC or Linux desktop replace the .bat file with a bash sh with the same java startup command.
